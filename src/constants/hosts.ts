export const BASE_URL = `${window.location.protocol}//${window.location.hostname}`;

export const FULL_BASE_URL = `${window.location.protocol}//${window.location.host}`;

export const SERVER_URL = `${BASE_URL}:4000`;

export const GRAPHQL_URL = `${SERVER_URL}/graphql`;
