export const BASE_URL: string = `${window.location.protocol}//${window.location.hostname}`;

export const FULL_BASE_URL: string = `${window.location.protocol}//${window.location.host}`;

export const SERVER_URL: string = `${BASE_URL}:4000`;

export const GRAPHQL_URL: string = `${SERVER_URL}/graphql`;
